package edu.knol.model

import java.util.UUID

case class Temperature(
                      deviceId: String,
                      timestamp: String,
                      temperature: Double
                      )
